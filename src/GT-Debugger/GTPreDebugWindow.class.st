"
There should be an implementation of the pre debug window in Glamour with no dependency to the one in spec
"
Class {
	#name : #GTPreDebugWindow,
	#superclass : #GLMCompositePresentation,
	#category : #'GT-Debugger'
}
